# The NEXT_PUBLIC_ prefix means this value is safe to expose in the browser.
# NEVER put secrets (passwords, JWT signing keys) in a NEXT_PUBLIC_ variable —
# anything with this prefix gets bundled into the JavaScript sent to every visitor.
NEXT_PUBLIC_API_URL="http://localhost:4000/api"
NEXT_PUBLIC_SOCKET_URL="http://localhost:4000"
